<%# Header %>
<section class="flex w-full">
  <div class="flex items-center justify-center w-full h-48 m-2 card bg-base-300 rounded-box">
    <div class="text-xl font-semibold">Současné skóre: <%= Current.user.total_score %></div>
    <div class="text-lg">Pořadí v žebříčku: <%= Current.user.leaderboard_position %></div>
  </div>

  <div class="flex items-center justify-center w-full m-2 card bg-base-300 rounded-box h-72">
    <%= image_tag "logo.png", class: "h-48" %>
    <div class="text-2xl">Vítejte, <%= Current.user.name %>!</div>
  </div>

  <div class="flex flex-row items-center justify-center w-full h-48 gap-4 m-2 card bg-base-300 rounded-box">
    <%= link_to edit_user_path(Current.user), class: "text-lg" do %>
      <%= Current.user.name %>
    <% end %>
    <%= link_to edit_user_path(Current.user) do %>
      <%= image_tag Current.user.avatar, class: "h-32 w-32 rounded-full" if Current.user.avatar.attached? %>
    <% end %>
  </div>
</section>

<%# Top players %>

<section class="flex w-full h-auto ">
  <div class="flex flex-row items-center justify-center w-full h-full gap-4 m-2 card bg-base-300 rounded-box min-h-96">
    <ul class="w-full h-full m-4 shadow-md list bg-base-100 rounded-box">
      <li class="p-4 pb-2 text-xs tracking-wide opacity-60">Top hráči</li>

      <% User.top_leaderboard.each.with_index(1) do |user, index| %>
        <li class="list-row">
          <div class="text-4xl font-thin opacity-30 tabular-nums"><%= index %></div>
          <div>
            <% if user.avatar.attached? %>
              <%= image_tag user.avatar, class: "size-10 rounded-box" %>
            <% end %>
          </div>
          <div class="list-col-grow"><%= user.name %></div>
          <div class="text-xs font-semibold uppercase opacity-60"><%= user.total_score %></div>
        </li>
      <% end %>
    </ul>
  </div>
  <div class="flex flex-col items-center justify-center w-full h-full gap-4 m-2 card bg-base-300 rounded-box min-h-96">
    <div>Text - úvod</div>
    <%= link_to "Začít hru", playthroughs_path, class: "btn btn-primary" %>
    <div>Text - pravidla</div>
  </div>
</section>
